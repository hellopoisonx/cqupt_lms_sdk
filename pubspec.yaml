name: cqupt_lms_sdk
description: 重庆邮电大学统一身份认证 (CAS) 与 LMS 签到 Dart SDK。
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.2
  crypto: ^3.0.5
  collection: ^1.18.0
  logging: ^1.3.0
  pointycastle: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
